# AAWS_project

이 프로젝트는 브라우저 자동화 및 에이전트 구동을 위한 환경을 제공합니다. 
GitHub Codespaces에서 바로 실행할 수 있도록 구성되어 있습니다.

## 🚀 시작하기 (환경 세팅)

Codespaces 환경을 처음 열었다면, 터미널에서 다음 명령어를 실행하여 필요한 모든 패키지(한글 폰트, noVNC 등)를 한 번에 설치할 수 있습니다.

```bash
# install 폴더로 이동하여 전체 설치 스크립트 실행
cd install
bash install_all.sh
```

설치가 완료되면 Python 패키지와 디스플레이 관련 프로그램, 한글 폰트 설정이 모두 자동으로 마무리되며 스크립트 실행 권한이 부여됩니다.

## 🖥 VNC 서버 실행

브라우저 동작을 눈으로 확인하기 위해 VNC 서버를 띄울 수 있습니다. 프로젝트 루트 폴더에서 아래 스크립트를 실행해 주세요.

```bash
# 프로젝트 루트로 돌아가서 실행
cd ..
./start_vnc.sh
```

실행 후 터미널에 나타나는 포트 포워딩 주소를 클릭하거나, 포트 탭에서 `6080` 포트로 연결되는 브라우저를 띄우면 프로젝트 내부 데스크톱 화면을 확인할 수 있습니다.